apiVersion: v1
kind: Service
metadata:
    name: sknirwalinventorymanagementsystem
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: sknirwalinventorymanagementsystem